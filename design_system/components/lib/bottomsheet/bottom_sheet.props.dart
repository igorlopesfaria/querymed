import 'package:flutter/widgets.dart';

class DSBottomSheetProps {
  DSBottomSheetProps({
    required this.child,
    required this.showDragIcon
  });

  final Widget child;
  final bool showDragIcon;
}
